{
  "name": "mad-makers-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Mad Makers — agence web créative · portfolio site (vanilla JS, no runtime build step)",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-projects.mjs",
    "minify": "node scripts/minify-assets.mjs",
    "optimize": "node scripts/optimize-images.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "sharp": "^0.33.5"
  }
}
